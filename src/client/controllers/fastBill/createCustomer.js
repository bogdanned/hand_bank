import fastBillCall from './fastBillCall'

export default async (order) => {
  // fetch customer by name
  // if not create customer
  // create a bil

  //let res = await fastBillCall(createCustomer)
  //return res
}
